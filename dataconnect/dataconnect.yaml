specVersion: "v1"
serviceId: "dc"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dc-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
